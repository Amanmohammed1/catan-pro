{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // No "DOM". No "@types/node". The engine cannot reference a browser or a
    // Node global even by accident — golden rule 1, enforced by the type system
    // rather than by review.
    "lib": ["ES2022"],
    "types": []
  },
  "include": ["src/**/*.ts"]
}
